[tool.ruff]
line-length = 88
indent-width = 4
target-version = "py310"
respect-gitignore = true
extend-exclude = [
    # ROS2 test scripts
    "src/**/test/*.py",

    # explicitly exclude submodules
    "src/doosan-robot2",
    "src/object-detection-node",
    "src/ObjRec_Patient_Mobility",
    "src/pmp-a0509",
    "src/zed-ros2-interfaces",
    "src/zed-ros2-wrapper"
]

[tool.ruff.lint]
extend-select = ["I"] # isort
